{
  "name": "web2025-6",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --host localhost --port 3000 --cache ./notes",
    "dev": "nodemon index.js --host localhost --port 3000 --cache ./notes",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "commander": "^14.0.0",
    "express": "^5.1.0",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
